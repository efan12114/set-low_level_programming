# C - Hash Tables

This project implements hash tables in C, covering creation, the djb2 hashing algorithm, key-to-index mapping, setting key-value pairs (with chaining for collisions), retrieving values, printing hash tables, deleting them, and an advanced sorted hash table implementation.
